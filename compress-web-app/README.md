# Compress Web App #

## Build & Run ##

```sh
$ cd Compress_Web_App
$ ./sbt
> jetty:start
> browse
```

If `browse` doesn't launch your browser, manually open [jett/](http://localhost:8080/) in your browser.
